import { featureWotrBundlePlanner } from './bundle-planner';

describe('featureWotrBundlePlanner', () => {
    it('should work', () => {
        expect(featureWotrBundlePlanner()).toEqual('feature-wotr-bundle-planner');
    });
});
