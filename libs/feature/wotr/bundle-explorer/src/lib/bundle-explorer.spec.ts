import { featureWotrBundleExplorer } from './bundle-explorer';

describe('featureWotrBundleExplorer', () => {
    it('should work', () => {
        expect(featureWotrBundleExplorer()).toEqual('feature-wotr-bundle-explorer');
    });
});
