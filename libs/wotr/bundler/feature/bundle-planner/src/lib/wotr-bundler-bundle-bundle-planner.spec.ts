import { wotrBundlerBundleBundlePlanner } from './wotr-bundler-bundle-bundle-planner';

describe('wotrBundlerBundleBundlePlanner', () => {
    it('should work', () => {
        expect(wotrBundlerBundleBundlePlanner()).toEqual('wotr-bundler-bundle-bundle-planner');
    });
});
