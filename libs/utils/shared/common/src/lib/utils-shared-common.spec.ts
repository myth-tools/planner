import { utilsSharedCommon } from './utils-shared-common';

describe('utilsSharedCommon', () => {
    it('should work', () => {
        expect(utilsSharedCommon()).toEqual('utils-shared-common');
    });
});
